#include <iostream>
#include <stdint.h>
#include <cassert>

// #define RELEASE

inline constexpr int N_ROWS = 19;
inline constexpr int N_COLS = N_ROWS;
inline constexpr int N_SQUARES = N_ROWS * N_COLS;